/**
 * Test the supabaseQuery class in databaseInterface.ts
 */

test('test',() => {
    expect(3).toBe(3)
})
//Create Test table/database once before all tests, then tear down
//test with incorrect database, should return error